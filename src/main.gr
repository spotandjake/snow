module Command

from "./libs/host.gr" include Host

// Wit re-export
@unsafe
provide let cabi_realloc = (originalPtr, originalSize, alignment, newSize) => {
  Host.cabi_realloc(originalPtr, originalSize, alignment, newSize)
}

// Program Entry
// fail "test"
provide let _start = () => {
  print("Hello, world!")
}
