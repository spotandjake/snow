module Nix

from "../libs/snow.gr" include Snow

let convertExpression = (expr: Snow.Expression) => {
  match (expr) {

  }
}
provide let fromNix = (nixAst: Snow.Expression) => {
  convertExpression(expr)
}
